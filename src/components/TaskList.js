import React from 'react';

const TaskList = () => {


    return (
        <div className="task-list">
            <p>Lista wyrenderowanych zadań :)</p>

            <button onClick={() => console.log()}>wyświetl</button>
        </div>
    );
}

export default TaskList;